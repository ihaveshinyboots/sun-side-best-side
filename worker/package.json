{
  "name": "sunside-onemap-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Thin Cloudflare Worker proxy: OneMap token middleman for sun-side-best-side.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
